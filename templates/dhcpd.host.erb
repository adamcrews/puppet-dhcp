host <%= @title %> {
  hardware ethernet   <%= @mac %>;
  fixed-address       <%= @ip %>;
  ddns-hostname       "<%= @title %>";
  option host-name    "<%= @title %>";
<% if @domain_name -%>
  option domain-name  "<%= @domain_name %>";
<% end -%>
}
